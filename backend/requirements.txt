# --- web ---
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
email-validator

# --- database ---
sqlalchemy>=2.0
alembic
psycopg[binary]

# --- auth ---
pyjwt
argon2-cffi

# --- config ---
python-dotenv

# --- ml runtime (inference) ---
numpy
scipy
scikit-learn
pandas
joblib
spacy
# Gradient boosting comes from scikit-learn's HistGradientBoostingClassifier,
# not LightGBM/XGBoost — no OpenMP native library to install, and a much
# smaller image. Explanations use the linear model's own coefficients, so SHAP
# (and its numba/llvmlite dependency chain) is not needed either.

# --- speech to text ---
faster-whisper

# --- mri pathway (inference only; training uses torch in ml/mri) ---
# Set MRI_ENABLED=false to skip loading these at runtime on tiny instances.
onnxruntime
nibabel
pillow
# DICOM ingestion: patients receive .dcm folders from scanning centres, not
# NIfTI — uploads of a zipped DICOM series are converted server-side.
dicom2nifti
pydicom
