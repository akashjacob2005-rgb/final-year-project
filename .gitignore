# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
frontend/dist/

# Secrets
.env
.env.local
.env.*.local

# Local database and user uploads
backend/data/
*.db
*.sqlite3

# Downloaded / derived data. Regenerate with:
#   python ml/download_data.py
ml/data/dementiabank_raw.json
ml/data/transcripts.csv
ml/data/features.csv

# DementiaBank Pitt corpus (audio + transcripts + derived ASR/features).
# Obtained under the TalkBank data-use agreement: it MUST NOT be committed,
# redistributed, or baked into a container image. Regenerate with your own
# credentials: python ml/download_pitt.py
ml/data/pitt/

# Trained model artefacts ARE committed — they are small (~123KB) and the
# Docker image ships them, so the app runs without retraining.
# ml/artifacts/

# OS
.DS_Store
Thumbs.db

# MRI training: the torch checkpoint is large and reproducible; only the ONNX
# export (which the backend serves) and metrics are committed.
ml/artifacts/mri_model.pt
